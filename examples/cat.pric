# -*- mode: ruby -*-

# STDIN=examples/cat.pric ./pricrun examples/cat.pric

def main()
  var EOF = -1;
  var c;

  while (true)
    c = getchar();

    case
    when (c == EOF)
      break;
    else
      write(c, 1);
    end
  end
end
